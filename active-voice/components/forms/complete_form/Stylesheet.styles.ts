import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    loading: {
        marginTop: 10,
    },
    form: {
        width: '100%',
        maxWidth: 600,
    },
    title: {
        textAlign: 'center',
        marginBottom: 20,
    },
    imageContainer: {
        position: 'relative',
        width: 200,
        height: 400,
    },
});

export default styles;